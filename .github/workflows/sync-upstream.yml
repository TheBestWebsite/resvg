# .github/workflows/sync-upstream.yml
name: Sync with upstream

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git identity
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_SIGNING_KEY }}" > ~/.ssh/ci_signing_key
          chmod 600 ~/.ssh/ci_signing_key
          git config --global gpg.format ssh
          git config --global user.signingkey ~/.ssh/ci_signing_key
          git config --global commit.gpgsign true
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add upstream remote
        run: git remote add upstream https://github.com/linebender/resvg.git

      - name: Add exemption for v0.47.0
        run: git config --add remote.upstream.fetch '^refs/tags/v0.47.0'

      - name: Fetch upstream (including all tags)
        run: git fetch upstream --tags

      - name: Rebase onto upstream
        run: |
          git checkout main
          git rebase upstream/main
          git push --force-with-lease origin main

      - name: Push tags to your fork
        run: git push origin --tags
